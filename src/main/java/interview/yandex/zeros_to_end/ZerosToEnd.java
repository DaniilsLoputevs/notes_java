package interview.yandex.zeros_to_end;

/**
 * Есть отсортированный массив целых чисел.
 * Нужно все НУЛИ перенести в конце.
 * Порядок НЕ НУЛЕВЫХ элементов должен сохраниться.
 *
 * [1, 2, 0, 3, 0, 4] -> [1, 2, 3, 4, 0, 0]
 *
 * * нужно ИЗМЕНИТЬ входной массив.
 */
class ZerosToEndTemplate implements ZerosToEnd {
    
    public void zeroesToEnd(int[] nums) {
        // write code here...
    }
}

public interface ZerosToEnd {
    void zeroesToEnd(int[] nums);
}
